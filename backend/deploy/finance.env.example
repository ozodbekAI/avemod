APP_NAME="WB Data Core Backend"
APP_ENV=development
DEBUG=false
API_V1_PREFIX=/api/v1
CORS_ALLOWED_ORIGINS='[]'
CORS_ALLOW_ORIGIN_REGEX=.*
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS='["GET","POST","PUT","PATCH","DELETE","OPTIONS"]'
CORS_ALLOW_HEADERS='["*"]'

DATABASE_URL=postgresql+asyncpg://finance_app:replace-me@127.0.0.1:5432/wb_data_core
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=40
DATABASE_POOL_TIMEOUT_SECONDS=60
DATABASE_POOL_RECYCLE_SECONDS=1800
DATABASE_STATEMENT_TIMEOUT_MS=120000
DATABASE_LOCK_TIMEOUT_MS=20000
DATABASE_IDLE_IN_TRANSACTION_TIMEOUT_MS=300000

JWT_SECRET_KEY=replace-with-strong-access-secret
JWT_REFRESH_SECRET_KEY=replace-with-strong-refresh-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=14

# Keep this aligned with the source database if you restore encrypted WB tokens.
WB_TOKEN_ENCRYPTION_KEY=replace-with-fernet-key

ENABLE_SCHEDULER=false
SCHEDULER_TIMEZONE=UTC

DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=200
HEAVY_ENDPOINT_CACHE_TTL_SECONDS=600
MONEY_RESPONSE_SNAPSHOT_REFRESH_MINUTES=1440
MONEY_RESPONSE_SNAPSHOT_MAX_STALE_MINUTES=1440
MONEY_RESPONSE_SNAPSHOT_ACTIVE_DAYS=7

WB_HTTP_TIMEOUT=30
WB_HTTP_RETRY_ATTEMPTS=3
WB_HTTP_RETRY_BACKOFF_SECONDS=1
ANALYTICS_FUNNEL_BATCH_SIZE=20
FINANCE_DETAIL_PAGES_PER_RUN=3
STOCKS_PENDING_TASK_MAX_AGE_HOURS=6
SYNC_RUNNING_CURSOR_STALE_HOURS=6

# AI Operator optional modules. Keep safe defaults in staging/production.
REPUTATION_ENABLED=false
REPUTATION_BASE_URL=
REPUTATION_SHOP_MAP={}
REPUTATION_AI_DEFAULT_ENABLED=false
ENABLE_REPUTATION_PUBLISH=false
ENABLE_REPUTATION_WRITE_ACTIONS=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
OPENAI_TIMEOUT_SECONDS=60

CLAIMS_ENABLED=false
CLAIMS_BASE_URL=
ENABLE_CLAIMS_SUBMIT=false

GROUPING_ENABLED=false
ENABLE_GROUPING_MERGE=false
ENABLE_CARD_AUTO_APPLY=false

# Staging smoke uses BASE_URL, ACCESS_TOKEN, ACCOUNT_ID, and optional NM_ID
# from the operator shell, not from this service env file.
